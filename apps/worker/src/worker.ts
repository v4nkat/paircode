// Deliberately does not consume jobs until the sandbox adapter and authorization exist.
console.error('Execution worker is not implemented in Milestone 0. See docs/roadmap.md.');
process.exitCode = 1;
